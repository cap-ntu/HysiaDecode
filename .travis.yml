language: cpp
git:
    quiet: true
    depth: false

compiler: g++
dist: trusty
sudo: true
os: linux
env: PYTHON=3.6

env: cuda=true
before_install:
    - sudo apt-get install python3-dev
    - sudo apt-get install python-pip
    - sudo pip install pybind11

script: make
